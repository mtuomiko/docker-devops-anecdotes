version: 2.1
orbs:
  docker: circleci/docker@1.3.0
  heroku: circleci/heroku@1.1.1
workflows:
  build-push-deploy:
    jobs:
      - docker/publish:
          image: mtuomiko/docker-devops-anecdotes
          tag: 'latest'
      - heroku/push-docker-image:
          process-types: 'web'
      - heroku/release-docker-image:
          process-types: 'web'

